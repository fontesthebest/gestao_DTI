{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "npm run build && cd backend && npx prisma db push --accept-data-loss && npx prisma db seed && npm start",
        "restartPolicyType": "ON_FAILURE"
    }
}