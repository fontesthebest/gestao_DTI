{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "cd backend && npm run build && npm run migrate:prod && npx prisma db seed && npm start",
        "restartPolicyType": "ON_FAILURE"
    }
}