{
    "name": "gestao-dti-monorepo",
    "private": true,
    "scripts": {
        "start": "npm start --prefix backend",
        "build": "npm run build --prefix backend",
        "postinstall": "npm install --prefix backend"
    }
}